# Pretrained models

No pretrained model is supplied with the current scaffold. Reconstruction will
eventually use an approved inference bundle supplied with a release.

`--model PATH` is an optional override for a compatible pretrained inference bundle.
The current CLI only records that path; it does not load or download a model.
Store local bundles under `exports/` or another location of your choice.
