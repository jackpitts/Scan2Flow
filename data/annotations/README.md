# Training and review annotations

Store masks, feature classes, primitive parameters, adjacency labels, measured
dimensions, confidence, and label provenance here. Missing annotations must be
explicitly masked rather than interpreted as negative labels. Real annotations are
intentionally absent and ignored by Git.
