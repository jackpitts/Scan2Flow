# Exploratory notebooks

Use this directory for exploratory capture inspection, error analysis, and model
experiments. No notebooks are required to run the CLI. Move reusable transforms,
training loops, and metrics into the package with meaningful tests before relying
on them in a pipeline. Clear private data and bulky outputs before committing a
notebook; reference immutable dataset and model versions instead.
