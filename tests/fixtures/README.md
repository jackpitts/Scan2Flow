# Test fixtures

Place small redistributable synthetic images, point clouds, CAD solids, and manifest
examples here when their adapters are implemented. Record units, provenance, and
expected acceptance criteria for each fixture. Do not add production captures,
large datasets, credentials, or model checkpoints to Git.

Current CLI tests create temporary files and do not require geometry fixtures.
