# Local output files

Choose an output directory with `--output`:

- `reconstruction/`: planned object STEP/STL geometry and quality reports.
- `cfd/`: planned fluid-domain geometry and boundary information.

The current scaffold creates no outputs. Dry-run commands only print a plan.
Generated files in this directory are excluded from version control.
