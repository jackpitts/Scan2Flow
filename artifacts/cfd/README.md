# CFD preparation outputs

Future runs write fluid-region geometry and `boundary_patches.json`, followed by
geometry and patch validation reports. Solver boundary conditions, volume meshes,
and simulation results are separate downstream artifacts and are not generated here.
