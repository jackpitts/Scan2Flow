# Reconstruction outputs

Future output bundles contain `object.step`/`object.stl` as requested, `quality.json`, input
and model provenance, and normalization transforms. No generated geometry is supplied.
